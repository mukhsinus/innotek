---
import "../styles/home.css";
import QuoteBand from "./QuoteBand.astro";
import RackAssembly3D from "./RackAssembly3D.astro";
import SectionMeta from "./SectionMeta.astro";
import { HOME, HOME_STATS } from "../data/home";
import { SITE } from "../data/site";
import type { Lang } from "../i18n/ui";
import { hrefFor, useT } from "../i18n/utils";

interface Props {
  lang: Lang;
}

const { lang } = Astro.props;
const t = useT(lang);
const copy = HOME[lang];
const quoteHref = `${hrefFor(lang, "/contacts/")}#quote`;
const catalogHref = hrefFor(lang, "/catalog/");
const videoHref = hrefFor(lang, "/video/");
const isUz = lang === "uz";
const address = isUz ? SITE.address.uz : SITE.address.ru;
const heroLines = copy.h1.split("\n");
const pad = (n: number) => String(n).padStart(2, "0");
const SECTIONS = 6;
const img = (src: string) => {
  if (!src) return "";
  let clean = src
    .replace("/src/assets/images/", "/images/")
    .replace("/imagesWebp/", "/images/");
  if (!clean.startsWith("/images/")) {
    clean = clean.startsWith("/") ? `/images${clean}` : `/images/${clean}`;
  }
  return clean;
};
---

<!-- 01 Hero: static, confident, blueprint grid accent -->
<section class="relative isolate flex h-[100dvh] min-h-[100dvh] sm:h-[100vh] sm:min-h-[100vh] items-end overflow-hidden text-ink">
  <img
    src={img(copy.heroImage)}
    alt=""
    class="hero-photo absolute inset-0 h-full w-full object-cover"
    width="1600"
    height="1000"
    loading="eager"
    fetchpriority="high"
    decoding="async"
  />
  <div class="site-wrap relative z-10 px-6 sm:px-8 md:px-0 pt-24 pb-24 sm:pb-28 lg:pb-16">
    <div class="mb-6 flex flex-wrap items-center gap-3">
      <p class="t-overline">01 / {pad(SECTIONS)}</p>
      <div class="telemetry-pill" aria-label="System status">
        <span class="relative flex h-1.5 w-1.5">
          <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-cyan opacity-75"></span>
          <span class="relative inline-flex rounded-full h-1.5 w-1.5 bg-cyan"></span>
        </span>
        <span>PRODUCTION // TASHKENT</span>
      </div>
    </div>
    <div class="max-w-5xl">
      <h1 class="t-display">
        {
          heroLines.map((line, i) => (
            <span class="hero-line" style={`--i: ${i}`}>
              <span class="hero-line-inner">{line}</span>
            </span>
          ))
        }
      </h1>
      <p class="hero-lead t-lead">{copy.heroLead}</p>
      <div class="hero-actions mt-10 flex flex-wrap items-center gap-x-8 gap-y-4">
        <a class="btn-accent" href={quoteHref}>{t("cta.quote")}</a>
        <a class="text-link" href={catalogHref}>
          {t("cta.catalog")}
          <span class="link-arrow" aria-hidden="true">→</span>
        </a>
      </div>
    </div>
  </div>
  <p class="hero-stick">{t("home.heroStick")}</p>
  <div class="hero-scroll" aria-hidden="true">
    <span class="hero-scroll-line"></span>
  </div>
</section>

<!-- 02 Interactive 3D Scroll Assembly Sequence -->
<RackAssembly3D lang={lang} index={2} total={SECTIONS} />

<!-- 03 Catalog: Modern Engineered Bento Grid -->
<section id="catalog" class="section-y bg-paper" data-reveal>
  <div class="site-wrap">
    <SectionMeta kicker={t("home.kickerCatalog")} index={3} total={SECTIONS} />
    <div class="flex flex-col md:flex-row md:items-end md:justify-between gap-6 mb-12 lg:mb-16">
      <div>
        <h2 class="t-display">
          {copy.catalogHeading.before}
          <span class="t-stroke">{copy.catalogHeading.accent}</span>
        </h2>
        <p class="t-lead">{copy.catalogLead}</p>
      </div>
      <a class="text-link self-start md:self-end pb-1 font-mono text-xs text-cyan hover:text-accent" href={catalogHref}>
        <span>[ {isUz ? "BARCHA MAHSULOTLAR" : "ВЕСЬ КАТАЛОГ ПРОДУКЦИИ"} ]</span>
        <span class="link-arrow" aria-hidden="true">→</span>
      </a>
    </div>

    <!-- 12-Column Responsive Bento Grid (14 items distributed across 5 balanced dynamic rows: 2, 3, 2, 4, 3) -->
    <div class="grid grid-cols-1 md:grid-cols-12 gap-4 lg:gap-5">
      {
        copy.categories.map((cat, i) => {
          let spanClass = "col-span-12 md:col-span-6 lg:col-span-4";
          let heightClass = "h-32 sm:h-36 lg:h-40";
          let titleSize = "text-base sm:text-lg lg:text-xl";

          if (i === 0) {
            // Row 1 (2 items: 8 + 4): Flagship rack
            spanClass = "col-span-12 md:col-span-6 lg:col-span-8";
            heightClass = "h-44 sm:h-52 lg:h-56";
            titleSize = "text-xl sm:text-2xl";
          } else if (i === 1) {
            // Row 1 (2 items: 8 + 4)
            spanClass = "col-span-12 md:col-span-6 lg:col-span-4";
            heightClass = "h-44 sm:h-52 lg:h-56";
            titleSize = "text-lg sm:text-xl";
          } else if (i >= 2 && i <= 4) {
            // Row 2 (3 items: 4 + 4 + 4)
            spanClass = "col-span-12 md:col-span-6 lg:col-span-4";
            heightClass = "h-32 sm:h-36 lg:h-40";
            titleSize = "text-base sm:text-lg lg:text-xl";
          } else if (i === 5 || i === 6) {
            // Row 3 (2 items: 6 + 6)
            spanClass = "col-span-12 md:col-span-6 lg:col-span-6";
            heightClass = "h-36 sm:h-40 lg:h-44";
            titleSize = "text-lg sm:text-xl";
          } else if (i >= 7 && i <= 10) {
            // Row 4 (4 items: 3 + 3 + 3 + 3)
            spanClass = "col-span-12 md:col-span-6 lg:col-span-3";
            heightClass = "h-28 sm:h-32 lg:h-36";
            titleSize = "text-sm sm:text-base font-semibold";
          } else {
            // Row 5 (3 items: 4 + 4 + 4)
            spanClass = "col-span-12 md:col-span-6 lg:col-span-4";
            heightClass = "h-32 sm:h-36 lg:h-40";
            titleSize = "text-base sm:text-lg lg:text-xl";
          }

          return (
            <a
              href={hrefFor(lang, cat.path)}
              data-stagger
              data-spotlight
              style={`--stagger: ${i}`}
              class:list={[
                "group relative flex flex-col justify-between overflow-hidden rounded-xl border border-line bg-surface/70 p-4 sm:p-5 lg:p-6 transition-all duration-300 hover:border-accent/50 hover:bg-surface hover:shadow-md",
                spanClass,
              ]}
            >
              <span class="cad-cross cad-tl" aria-hidden="true">+</span>
              <span class="cad-cross cad-tr" aria-hidden="true">+</span>
              <span class="cad-cross cad-bl" aria-hidden="true">+</span>
              <span class="cad-cross cad-br" aria-hidden="true">+</span>

              <!-- Top index & arrow pill -->
              <div class="relative z-10 flex items-center justify-between">
                <span class="font-mono text-[11px] sm:text-xs font-semibold text-cyan tracking-wider">
                  {pad(i + 1)} // {i === 0 ? (isUz ? "FLAGMAN" : "ФЛАГМАН") : (isUz ? "KATEGORIYA" : "КАТЕГОРИЯ")}
                </span>
                <span class="inline-flex items-center justify-center w-7 h-7 sm:w-8 sm:h-8 rounded-full border border-line bg-paper text-muted-on-ink group-hover:border-accent group-hover:text-accent group-hover:translate-x-1 transition-all duration-300 shadow-xs text-xs sm:text-sm">
                  →
                </span>
              </div>

              <!-- Product Visual Container (Clean crisp image, no washout) -->
              <div class:list={[
                "relative my-3 sm:my-4 flex items-center justify-center overflow-hidden rounded-lg bg-paper/60 p-3 sm:p-4 transition-all duration-500 group-hover:bg-paper",
                heightClass,
              ]}>
                <img
                  src={img(cat.image)}
                  alt={t(cat.labelKey)}
                  class="h-full w-full object-contain filter contrast-[1.03] brightness-[0.98] transition-transform duration-500 group-hover:scale-105"
                  width="600"
                  height="400"
                  loading={i < 2 ? "eager" : "lazy"}
                  decoding="async"
                />
              </div>

              <!-- Bottom Category Title -->
              <div class="relative z-10 pt-1">
                <h3 class:list={[
                  "font-display font-semibold tracking-tight text-ink group-hover:text-accent transition-colors leading-snug",
                  titleSize,
                ]}>
                  {t(cat.labelKey)}
                </h3>
              </div>

              <!-- Left Orange Accent Strip on Hover -->
              <span class="absolute left-0 top-0 bottom-0 w-[3px] bg-accent opacity-0 group-hover:opacity-100 transition-opacity duration-300"></span>
            </a>
          );
        })
      }
    </div>
  </div>
</section>

<!-- 04 Production stats -->
<section id="production" class="section-y bg-paper" data-reveal>
  <div class="site-wrap">
    <SectionMeta kicker={t("home.kickerProduction")} index={4} total={SECTIONS} />
    <h2 class="t-display">
      {copy.productionHeading.before}
      <span class="t-stroke">{copy.productionHeading.accent}</span>
    </h2>
    <p class="t-lead">{copy.productionLead}</p>

    <!-- 4 High-End Telemetry Stat Cards -->
    <div class="grid grid-cols-1 gap-5 sm:grid-cols-2 lg:grid-cols-4 mt-12 lg:mt-16">
      <!-- Card 1: Experience -->
      <div class="stat-card group flex flex-col justify-between p-6 sm:p-7" data-stagger data-spotlight style="--stagger: 0">
        <span class="cad-cross cad-tl text-accent" aria-hidden="true">+</span>
        <span class="cad-cross cad-tr text-accent" aria-hidden="true">+</span>
        <span class="cad-cross cad-bl text-accent" aria-hidden="true">+</span>
        <span class="cad-cross cad-br text-accent" aria-hidden="true">+</span>

        <div>
          <p class="text-xs font-mono text-cyan tracking-wider font-semibold mb-3">
            01 // {isUz ? "TAJRIBA" : "ОПЫТ"}
          </p>
          <p class="text-3xl sm:text-4xl lg:text-[2.25rem] xl:text-[2.6rem] font-display font-bold text-ink tracking-tight leading-none">
            <span data-count={HOME_STATS.years}>{HOME_STATS.years}</span>
          </p>
        </div>
        <p class="text-xs font-mono font-bold uppercase tracking-wider text-muted-on-ink mt-4 pt-2 border-t border-line/60">
          {isUz ? "Yillik tajriba" : "Годы опыта"}
        </p>
      </div>

      <!-- Card 2: Clients -->
      <div class="stat-card group flex flex-col justify-between p-6 sm:p-7" data-stagger data-spotlight style="--stagger: 1">
        <span class="cad-cross cad-tl text-accent" aria-hidden="true">+</span>
        <span class="cad-cross cad-tr text-accent" aria-hidden="true">+</span>
        <span class="cad-cross cad-bl text-accent" aria-hidden="true">+</span>
        <span class="cad-cross cad-br text-accent" aria-hidden="true">+</span>

        <div>
          <p class="text-xs font-mono text-cyan tracking-wider font-semibold mb-3">
            02 // {isUz ? "MIJOZLAR" : "КЛИЕНТЫ"}
          </p>
          <p class="text-3xl sm:text-4xl lg:text-[2.25rem] xl:text-[2.6rem] font-display font-bold text-ink tracking-tight leading-none whitespace-nowrap">
            <span data-count={HOME_STATS.clients}>{HOME_STATS.clients}</span><span class="text-accent ml-0.5 font-light">+</span>
          </p>
        </div>
        <p class="text-xs font-mono font-bold uppercase tracking-wider text-muted-on-ink mt-4 pt-2 border-t border-line/60">
          {isUz ? "Mamnun mijozlar" : "Счастливый клиент"}
        </p>
      </div>

      <!-- Card 3: Partners -->
      <div class="stat-card group flex flex-col justify-between p-6 sm:p-7" data-stagger data-spotlight style="--stagger: 2">
        <span class="cad-cross cad-tl text-accent" aria-hidden="true">+</span>
        <span class="cad-cross cad-tr text-accent" aria-hidden="true">+</span>
        <span class="cad-cross cad-bl text-accent" aria-hidden="true">+</span>
        <span class="cad-cross cad-br text-accent" aria-hidden="true">+</span>

        <div>
          <p class="text-xs font-mono text-cyan tracking-wider font-semibold mb-3">
            03 // {isUz ? "HAMKORLAR" : "ПАРТНЕРЫ"}
          </p>
          <p class="text-3xl sm:text-4xl lg:text-[2.25rem] xl:text-[2.6rem] font-display font-bold text-ink tracking-tight leading-none">
            <span data-count={HOME_STATS.partners}>{HOME_STATS.partners}</span>
          </p>
        </div>
        <p class="text-xs font-mono font-bold uppercase tracking-wider text-muted-on-ink mt-4 pt-2 border-t border-line/60">
          {isUz ? "Hamkorlar va yetkazib beruvchilar" : "Партнеров и поставщиков"}
        </p>
      </div>

      <!-- Card 4: Production Area -->
      <div class="stat-card group flex flex-col justify-between p-6 sm:p-7" data-stagger data-spotlight style="--stagger: 3">
        <span class="cad-cross cad-tl text-accent" aria-hidden="true">+</span>
        <span class="cad-cross cad-tr text-accent" aria-hidden="true">+</span>
        <span class="cad-cross cad-bl text-accent" aria-hidden="true">+</span>
        <span class="cad-cross cad-br text-accent" aria-hidden="true">+</span>

        <div>
          <p class="text-xs font-mono text-cyan tracking-wider font-semibold mb-3">
            04 // {isUz ? "MAYDON" : "ПЛОЩАДЬ"}
          </p>
          <p class="text-3xl sm:text-4xl lg:text-[2.25rem] xl:text-[2.6rem] font-display font-bold text-ink tracking-tight leading-none">
            <span data-count={HOME_STATS.area}>{HOME_STATS.area}</span>
          </p>
        </div>
        <p class="text-xs font-mono font-bold uppercase tracking-wider text-muted-on-ink mt-4 pt-2 border-t border-line/60">
          {isUz ? "Ishlab chiqarish maydoni (kv.m)" : "Производственных площадей (кв.м)"}
        </p>
      </div>
    </div>

    <!-- Factory Highlights Ribbon -->
    <div class="mt-8 grid grid-cols-1 gap-4 border-t border-line/80 pt-8 sm:grid-cols-3">
      <div class="flex items-center gap-3 text-xs font-mono text-muted-on-ink">
        <span class="text-accent font-bold">✓</span>
        <span>{isUz ? "Oylik quvvat: 500+ tonna po'lat" : "Производственная мощность: 500+ т/мес"}</span>
      </div>
      <div class="flex items-center gap-3 text-xs font-mono text-muted-on-ink">
        <span class="text-accent font-bold">✓</span>
        <span>{isUz ? "ISO 9001:2015 sifat nazorati" : "Сертификация качества ISO 9001:2015"}</span>
      </div>
      <div class="flex items-center gap-3 text-xs font-mono text-muted-on-ink">
        <span class="text-accent font-bold">✓</span>
        <span>{isUz ? "Toshkentdagi o'z zavodimiz" : "Собственный завод и склад в Ташкенте"}</span>
      </div>
    </div>
  </div>
</section>

<!-- 05 Cases -->
<section id="cases" class="section-y bg-surface" data-reveal>
  <div class="site-wrap">
    <SectionMeta kicker={t("home.kickerCases")} index={5} total={SECTIONS} />
    <h2 class="t-display">
      {copy.casesHeading.before}
      <span class="t-stroke">{copy.casesHeading.accent}</span>
    </h2>
    <p class="t-lead">{copy.casesLead}</p>

    <!-- Modern Fresh Case Cards Grid -->
    <div class="mt-12 grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3 lg:mt-16">
      {
        copy.cases.map((item, i) => (
          <a
            href={videoHref}
            class="group relative flex flex-col justify-between overflow-hidden rounded-xl border border-line bg-paper p-6 sm:p-7 shadow-xs transition-all duration-300 hover:border-accent/50 hover:shadow-lg"
            data-stagger
            data-spotlight
            style={`--stagger: ${i}`}
          >
            <span class="cad-cross cad-tl" aria-hidden="true">+</span>
            <span class="cad-cross cad-tr" aria-hidden="true">+</span>
            <span class="cad-cross cad-bl" aria-hidden="true">+</span>
            <span class="cad-cross cad-br" aria-hidden="true">+</span>

            <!-- Top Index & Arrow -->
            <div class="relative z-10 flex items-center justify-between">
              <span class="font-mono text-xs font-semibold text-cyan tracking-wider">
                {pad(i + 1)} //
              </span>
              <span class="inline-flex h-8 w-8 items-center justify-center rounded-full border border-line bg-surface text-muted-on-ink transition-all duration-300 group-hover:border-accent group-hover:text-accent group-hover:translate-x-1 shadow-xs">
                →
              </span>
            </div>

            <!-- Product Visual Container -->
            <div class="relative my-5 flex aspect-[4/3] items-center justify-center overflow-hidden rounded-lg bg-surface/60 p-4 transition-all duration-500 group-hover:bg-surface">
              <img
                src={img(item.image)}
                alt={t(item.labelKey)}
                class="h-full w-full object-contain filter contrast-[1.03] brightness-[0.98] transition-transform duration-500 group-hover:scale-106"
                width="600"
                height="450"
                loading="lazy"
                decoding="async"
              />
            </div>

            <!-- Bottom Label -->
            <div class="relative z-10 pt-1">
              <p class="font-display text-lg font-semibold tracking-tight text-ink transition-colors group-hover:text-accent">
                {t(item.labelKey)}
              </p>
            </div>

            <!-- Left Orange Accent Line on Hover -->
            <span class="absolute left-0 top-0 bottom-0 w-[3px] bg-accent opacity-0 transition-opacity duration-300 group-hover:opacity-100"></span>
          </a>
        ))
      }
    </div>

    <div class="mt-12 lg:mt-16">
      <a class="btn-accent" href={videoHref}>{t("cta.video")}</a>
    </div>
  </div>
</section>

<!-- 06 CTA -->
<QuoteBand lang={lang} index={6} total={SECTIONS} />


